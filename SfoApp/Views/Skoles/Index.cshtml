@model IEnumerable<SfoApp.Models.Skole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SkoleNavn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SkoleNavn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SkoleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SkoleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SkoleId })
        </td>
    </tr>
}

</table>
